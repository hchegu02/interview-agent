# Tasks

- [x] 1. 扩展 `StatePatch` 支持当前轮追问追加和 CriticResult 追问信号更新。
- [x] 2. 补 `ApplyStatePatch` 单测。
- [x] 3. 新增 `NewProbeAskPatchNode`，旧 `NewProbeAskNode` 保留 wrapper。
- [x] 4. 将 Interview Graph 中 `probe_ask` 注册为 `PatchNode`。
- [x] 5. 补 probe_ask patch/suspend 和降级测试。
- [x] 6. 更新 SDD、code-changes 和 OpenSpec，并运行验证。
