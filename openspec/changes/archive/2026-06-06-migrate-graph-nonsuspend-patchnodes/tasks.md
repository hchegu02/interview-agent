# Tasks

- [x] 1. 新增非挂起节点 patch 构造函数，并保留 legacy wrapper。
- [x] 2. 将 `BuildInterviewGraph` 中 `retrieve_rag`、`evaluate`、`report` 注册为 runner-level `PatchNode`。
- [x] 3. 补测试覆盖 patch 节点 direct runner apply、hook summary 和 `WorkingMemory` 降级 patch。
- [x] 4. 更新 SDD 与 `docs/code-changes` 过程文档。
- [x] 5. 运行 OpenSpec 和 Go 验证。
