# Tasks

- [x] 1. 新增 `NewUpdateMemoryPatchNode`，旧 `NewUpdateMemoryNode` 保留 wrapper。
- [x] 2. 将 Interview Graph 中 `update_memory` 注册为 `PatchNode`。
- [x] 3. 补 update_memory patch 成功和降级轮次测试。
- [x] 4. 更新 SDD、code-changes 和 OpenSpec，并运行验证。
