# Tasks

- [x] 1. 扩展 `StatePatch` 支持当前轮 RefinedEval 写入。
- [x] 2. 补 `ApplyStatePatch` 单测。
- [x] 3. 新增 `NewRefinePatchNode`，旧 `NewRefineNode` 保留 wrapper。
- [x] 4. 将 Interview Graph 中 `refine` 注册为 `PatchNode`。
- [x] 5. 补 refine patch 成功、无需 refine、LLM 降级测试。
- [x] 6. 更新 SDD、code-changes 和 OpenSpec，并运行验证。
