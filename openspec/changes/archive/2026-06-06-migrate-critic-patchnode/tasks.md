# Tasks

- [x] 1. 扩展 `StatePatch` 支持当前轮完整 CriticResult 写入。
- [x] 2. 补 `ApplyStatePatch` 单测。
- [x] 3. 新增 `NewCriticPatchNode`，旧 `NewCriticNode` 保留 wrapper。
- [x] 4. 将 Interview Graph 中 `critic` 注册为 `PatchNode`。
- [x] 5. 补 critic patch 成功、降级评估、LLM 降级测试。
- [x] 6. 更新 SDD、code-changes 和 OpenSpec，并运行验证。
