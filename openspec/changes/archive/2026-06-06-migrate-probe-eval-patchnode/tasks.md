# Tasks

- [x] 1. 扩展 `StatePatch` 支持当前最后追问 Evaluation 写入。
- [x] 2. 补 `ApplyStatePatch` 单测。
- [x] 3. 新增 `NewProbeEvalPatchNode`，旧 `NewProbeEvalNode` 保留 wrapper。
- [x] 4. 将 Interview Graph 中 `probe_eval` 注册为 `PatchNode`。
- [x] 5. 补 probe_eval patch 成功、空答案、降级测试。
- [x] 6. 更新 SDD、code-changes 和 OpenSpec，并运行验证。
