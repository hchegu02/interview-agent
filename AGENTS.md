# Codex 项目工作规则

## 项目定位

Interview Agent 是基于 Go 后端、Agent Graph、RAG、多轮评分和报告生成的 AI 模拟面试系统。当前项目重点是后端工程实现与优化，前端是候选人工作台和后端能力展示层。

架构说明以以下文档为准：

- `docs/SDD-Backend.md`
- `docs/SDD-Frontend.md`

## 语言与注释

- 默认使用简体中文回复。
- 新增代码注释默认使用中文，但不为了统一语言去翻译既有注释。
- 标识符、API 字段、日志、用户可见文案遵循项目现有命名和语言风格。

## 工作原则

- 先读后写：修改前先阅读 SDD、相关代码和现有测试。
- 先定位入口：必须说明涉及的代码路径和当前实现。
- 先计划再实现：复杂功能先走 OpenSpec / Comet，不直接大改代码。
- 小步修改：只改当前任务需要的文件，不做无关重构。
- 接口兼容：新增 JSON 字段优先 `omitempty`，避免破坏旧 Session、HTTP 响应和前端类型。
- 后端优先：业务状态、Graph、RAG、LLM、存储和验证门禁是主线。
- 不夸大能力：Codex sub-agent 是开发协作方式，不是当前项目运行时能力。

## 开发流程

1. 阅读需求、`docs/SDD-Backend.md` 和相关代码。
2. 输出不确定点、默认决策、风险点和测试计划。
3. 参考 `docs/ai/modules.md` 定位代码入口，说明要新增或修改的文件。
4. 实现前优先补测试或明确验证点。
5. 修改后运行最小必要验证。
6. 如果改变架构、接口或运行链路，更新 SDD 或 OpenSpec。

## 阶段收口流程

每个 OpenSpec change 必须按以下顺序收口，避免代码、规格和 Git 状态分裂：

```text
proposal/design/tasks
  -> 先补测试或明确验证点
  -> 实现
  -> go test ./...
  -> openspec validate <change> --strict
  -> openspec archive <change> --yes
  -> openspec validate <capability> --strict
  -> 精确 git add
  -> git diff --cached --name-status
  -> git commit
  -> 阶段性 push
```

例外：

- 纯文档或配置微调可以跳过完整 OpenSpec，但仍要说明影响范围和验证方式。
- 归档后如果主 spec 和代码行为不一致，必须先修 spec 或代码，再提交。
- 本地分支长期 ahead 时，优先收口、提交和 push，不继续叠大功能。

## 禁止事项

- 不使用 `git add .`，提交时必须精确暂存文件。
- 不提交 `.env`、token、密钥、私有配置和 `.vscode`。
- 不把 `docs/code-changes`、`docs/superpowers`、`docs/项目讲解` 作为默认提交范围。
- 不让 LLM 绕过后端 schema、权限、超时和验证边界直接控制流程。
- 不把未实现的 LangGraph、sub-agent runtime、Gateway、daemon、Sandbox 写成当前能力。

## 代码变更文档规则

只要本次任务新增、修改或删除会改变运行时行为、接口契约、数据流、存储、依赖、配置或验证链路的代码，必须同步创建或更新：

```text
docs/code-changes/MM-DD-简短变更名.md
```

文档必须基于真实代码 diff，不得编造。必须包含：

1. 变更概述：说明改了什么、为什么改、影响范围。
2. 变更文件：列出所有新增、修改、删除的代码文件及其作用。
3. 函数级说明：逐个说明所有新增或修改的函数、方法、组件、Hook、API handler、导出符号，包括位置、作用、输入、输出、副作用、错误处理、主要逻辑和行为变化。
4. 调用链：对每个新增或行为变化的函数，从最高层入口写到目标函数。入口包括用户操作、API 路由、CLI、定时任务、事件监听器、应用启动、测试入口或外部回调。只能写源码中确认的调用关系；无法确认时标注“调用链部分确认”。
5. 数据流：说明关键数据的来源、校验、转换、传递、存储和返回。
6. 依赖与副作用：说明 import、外部 API、数据库、文件系统、环境变量、缓存、日志、全局状态和安全相关影响。
7. 测试：记录新增或修改的测试、执行命令、结果；未测试则说明原因。
8. 风险：说明兼容性、性能、安全、并发和已知限制。

完成代码任务前，必须确认文档已同步更新。

例外：

- 纯注释修改不需要新增或更新 `docs/code-changes`。
- 纯格式化且不改变语义不需要新增或更新 `docs/code-changes`。
- 纯文档任务不需要新增 `docs/code-changes`。

## 验证要求

根据改动范围选择验证：

- Go 后端：`go test ./...`
- 前端：`npm --prefix web run test`、`npm --prefix web run build`
- RAG：`go run ./cmd/rag-eval ...`
- Agent 输出：`go run ./cmd/agent-verify -session testdata/agent_verify/pass_session.json`

如果没有运行验证，必须明确说明未验证原因。

## 多 Agent 使用规则

这里的多 Agent 指 Codex 开发协作中的 sub-agent / 并行审查，不代表 Interview Agent 项目运行时已经具备多 Agent runtime 能力。

### 允许使用

- 多个问题相互独立，且可以并行阅读、调查或验证。
- 多个测试文件失败，根因可能分属不同模块。
- 需要独立视角做代码审查、回归风险检查、安全风险检查或测试建议。
- 大型实现计划中存在明确的非重叠文件或模块范围。
- 文档、测试、实现、风险审查可以拆成互不阻塞的任务。

### 不应使用

- 任务很小，单线程完成更快。
- 根因未定位，多个现象可能来自同一个 bug。
- 需要统一理解全局状态、架构决策或业务语义。
- 多个 agent 会修改同一文件、同一接口、同一数据结构或同一状态流。
- 需求仍不清楚，拆给 agent 只会放大误解。
- 只是为了显得“智能”而增加协作复杂度。

### 使用要求

- 主 agent 必须保留协调权，不能把关键路径完全外包。
- 每个 sub-agent 必须有明确边界：目标、涉及文件、禁止修改范围、期望输出。
- sub-agent 不应继承完整会话上下文；只提供完成任务所需的最小上下文。
- 并行 agent 的文件范围必须尽量不重叠；如不可避免，先说明冲突风险。
- sub-agent 输出必须包含：已检查文件、结论、修改内容、验证结果、剩余风险。
- sub-agent 的结论不能替代真实测试、构建、浏览器检查或人工集成审查。
- 集成前主 agent 必须复核所有改动，确认没有接口冲突、重复实现或行为漂移。

### 推荐并发模式

优先使用以下三类并发任务：

- 代码审查 agent：检查 bug、兼容性、测试缺口和边界条件。
- 规格审查 agent：检查 OpenSpec、SDD、tasks 和真实代码是否一致。
- 下一阶段调研 agent：只读分析后续入口和风险，不直接改文件。

实现型 sub-agent 只在文件范围不重叠时使用；涉及同一接口、同一状态结构或同一 Graph 流程时，由主 agent 本地实现。
